# antislop audit 001 — 2026-09-21 (Mode 2: audit first)

Scope: the four approved batches (study/exam surfaces, parent portal, systemic
token pass, image-led landing). Direction: stay in the current terracotta
LiquidGlass system (owner-confirmed). Dials: ENERGY 2 / RHYTHM 2 / MOTION 2,
matching the existing GSAP reveal + count-up language already in the app.

Contrast ratios below were computed with
`C:\Users\Xe0n0\.agents\skills\antislop-human\contrast-check.py`, not eyeballed.

## Already passing (no finding filed)

- Fonts, single accent, no AI gradients, grain/glass tokens, tabular-nums,
  favicon/meta, honest nulls in SessionReportView, honest empty states in
  parent portal (OverviewTab:345, PairingCard:195, VaultTab:223,
  AuditLogPanel:85, ParentAnalytics:66/140/205), specific CTAs
  ("Back to Study Room", "View Achievements"), keyboard arrow-nav on parent
  tabs, no fake stats/testimonials in scope (R-17/R-18/R-36 PASS).
- Muted slate #94A3B8 on obsidian #06080C = 7.82:1 PASS; light muted #6B7280
  on #F6F7F9 = 4.51:1 PASS (borderline, keep an eye on it).

## Recorded overrides (R-37 conflict protocol, owner kept)

- **R-10 glass dose:** glass is applied page-wide (navbar/cards/modals/sidebar),
  over the 1-2 element cap. Owner chose "stay in current system": glass IS the
  product identity here, with a documented reason (optical-depth design
  language, globals.css:6-14). No change.
- **R-04 Lucide:** library-exclusive icons kept; swapping the set is rewrite
  risk for zero hierarchy gain. Relevance per icon is fine.
- **R-08 single arrow:** one ChevronRight on "View Achievements"
  (SessionReportView:169) kept: forward-navigation direction cue, proportional,
  purpose written here.

## Findings

### HIGH (Hard Gate)

1. **R-25 FAIL: white small text on terracotta primary is 3.26:1** (needs 4.5).
   Affects `glass-btn-primary` everywhere in dark theme
   (glass-surfaces.css:81). Fix: darken the button background for text use
   (e.g. shift toward #B85A38 already used in FloatingGuru:333) or use dark
   text on the primary fill; verify with contrast-check.py.
2. **R-25 FAIL: light-theme primary #C8613C on porcelain #F6F7F9 is 3.75:1**
   for normal text (globals.css:74). Fix: darken the light primary used for
   text/links (buttons with white text pass at large/bold sizes only).
3. **R-32 weak focus:** playground inputs use `outline-none` + border-only
   focus (`focus:border-[var(--primary)]/40`, playground/page.tsx:469 and ~15
   siblings) with no ring, while auth pages use `focus:ring-2`. A 40%-opacity
   border shift alone is a weak indicator. Fix: standardize on the auth
   ring pattern (visible, 3:1) in both themes.

### MEDIUM (Purpose-Gate / consistency)

4. **R-27 loading states are spinner-only:** SessionReportView:91 (`Loader2` +
   "Compiling your session report…"), SittingRunner:303 ("Preparing your
   paper…"), VaultTab:218, LiveVideoView:509 (bare spinner). Fix: replace the
   two data-loading moments (report bento grid, paper prep) with
   layout-shaped skeleton cells; keep inline spinners for button-busy states
   (SittingRunner:358/615/751, PinLock:118) where they mark an action, not a view.
5. **R-27 empty state names no cause/action:** SittingRunner:596 "No questions
   in this paper." Fix: say why (empty paper / failed extraction) and the one
   next action (re-import, pick another paper).
6. **R-09/R-13 badge stack:** SessionReportView:146-152 badge pills combine
   capsule + thin border + glow bg + outer glow shadow all at once. This is the
   reward-celebration moment (purpose written), but the dose exceeds the cap.
   Fix: keep matte fill + border, drop the outer `shadow-[0_0_16px...]`, keep
   glow for the single newest badge only.
7. **No z-index scale:** arbitrary values scattered — z-[9999]/z-[9998]
   (FloatingGuru:333/344/368/404), z-[10000] (ProfileSetupModal:186,
   FirstRunGate:183), z-[200] (ToastViewport:45), z-[120]
   (VisualizationViewer:518), z-50 for all modals. One new overlay can
   silently lose to the bubble. Fix: token scale in CSS
   (modal < toast < floating < onboarding) and migrate these call sites.

### LOW (copy hygiene, in files already being touched)

8. **R-02 em dashes in user-facing strings:** study-room/page.tsx:333, 357,
   363, 370, 406, 413-414; PairingCard:195. Fix: comma/period restructure,
   only in files Batch 1-2 already edits (no repo-wide scrub).
9. **Success-message exclamation:** SessionReportView:74 "Session Complete!"
   Fix: "Session complete" (confident, not loud).

## Open decisions (not numbered fixes)

- **D1 — Batch 4 landing:** no root landing page exists (`web/app/page.tsx`
  absent), so image-led landing is a NEW page, not an upgrade. Before
  generating (R-23/R-24/R-37): confirm sections + copy source (no fabricated
  stats/testimonials per R-17/R-18), nav destinations that exist, and whether
  reference images are placeholders or final art.

## Resolutions (2026-09-21, all 9 approved and fixed)

1. Dark `--primary` #E06D44 to #B94E28 (white text 5.02:1). All primary-derived
   alphas (glow, ember, ring, accent, sheen, canvas, lg-rim) re-based; hardcoded
   component hexes migrated to tokens or new values (exam banner, playground
   tab, BookProgressTimeline, ThemePreviewCard palettes+preview, showcase,
   AppleGlass glare, PiP overlay strings, FloatingGuru gradient).
2. Light `--primary` #C8613C to #A34A28 (on porcelain 5.49:1, white on it 5.89:1).
3. 15 playground inputs gained `focus:ring-2 focus:ring-[var(--primary)]`
   (auth-page language, app-wide single focus style).
4. SessionReportView loading and SittingRunner paper-prep now render
   layout-shaped skeletons (`role=status`, `aria-busy`, sr-only text).
   Button-busy spinners intentionally kept (they mark actions, R-26).
5. SittingRunner empty paper now states cause + next action ("Back to papers"
   via existing onExit).
6. Badges matte except newest keeps the glow (single focus accent, R-13).
7. `--z-*` scale added to globals.css; arbitrary z-[9999]/[9998]/[10000]/[200]/
   [120] migrated with identical values (zero visual change). Plain z-50
   modals left (already systematic).
8. Em dashes removed from study-room strings (6) and PairingCard (1).
9. "Session Complete!" to "Session complete".

Verification: `tsc --noEmit` clean, node tests 437/437 pass, eslint 0 errors
(54 pre-existing tolerated warnings), `npm run build` succeeds. Contrast
recomputed with contrast-check.py (5.02 / 5.49 / 5.89, all PASS).
